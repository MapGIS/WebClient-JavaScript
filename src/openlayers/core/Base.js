import * as ol from 'ol';
/**
 *MapGIS WebClient Leaflet基类
 * 定义命名空间
 * 提供公共模块
 */

// let ol = ol || window.ol || {}

ol.zondy = ol.zondy || {};
ol.zondy.control = ol.zondy.control || {};

export default ol;

// ol.CRS = ol.CRS || {};
